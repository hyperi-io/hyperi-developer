---
# Shell configuration (PATH, bash history auto-commit, etc.)

# ============================================================================
# PATH CONFIGURATION (SYSTEM-WIDE)
# Add common tool directories to PATH for all users
# ============================================================================
# The directories are under $HOME, but the POLICY is system-wide: one file
# covers every user on the box, including ones created later, and it is read by
# login shells so non-interactive children inherit it. Per-user rc files cannot
# do either.
#
# Each entry MUST test PATH before prepending. /etc/profile is re-read by every
# login shell, so an unguarded prepend stacks a fresh copy per nested shell, per
# tmux pane, per `su -`.
- name: Configure PATH additions system-wide (Linux)
  ansible.builtin.copy:
    dest: /etc/profile.d/dev-path.sh
    content: |
      # Developer Environment PATH additions
      # System-wide configuration for all users
      #
      # Managed by hyperi-developer -- soe/tasks/shell_config.yml.

      # Prepend only if the directory exists AND is not already on PATH.
      hyperi_path_prepend() {
          [ -d "$1" ] || return 0
          case ":$PATH:" in
              *":$1:"*) ;;
              *) PATH="$1:$PATH" ;;
          esac
      }

      hyperi_path_prepend "$HOME/.cargo/bin"      # rustup and cargo-installed tools
      hyperi_path_prepend "$HOME/.local/bin"      # uv tools, pipx, gext
      hyperi_path_prepend "$HOME/.npm-global/bin" # npm global tools (semantic-release)

      export PATH
      unset -f hyperi_path_prepend
    mode: '0644'
  when: ansible_facts['distribution'] in ['Fedora', 'Ubuntu']

# zsh does not read /etc/profile.d on its own. Fedora's /etc/zshrc already
# sources it, and its /etc/zprofile sources /etc/profile, so Fedora needs
# nothing. Ubuntu's /etc/zsh/zprofile is comments only, which leaves a zsh user
# there with no dev-path.sh and no hyperi-go.sh.
#
# Only acts where zsh is installed -- this project never installs it on Linux.
# The file is a dpkg conffile, so the block carries a marker and touches nothing
# else.
- name: Check for the system zsh login config (Ubuntu)
  ansible.builtin.stat:
    path: /etc/zsh/zprofile
  register: shell_zsh_zprofile
  when: ansible_facts['distribution'] == 'Ubuntu'

- name: Source /etc/profile.d from zsh login shells (Ubuntu)
  ansible.builtin.blockinfile:
    path: /etc/zsh/zprofile
    marker: "# {mark} HYPERI: /etc/profile.d for zsh"
    block: |
      # zsh skips /etc/profile.d; these are sh scripts, so run them under sh
      # emulation to keep zsh's own parsing rules out of it.
      for hyperi_profile_script in /etc/profile.d/*.sh; do
          [ -r "$hyperi_profile_script" ] && emulate sh -c ". $hyperi_profile_script"
      done
      unset hyperi_profile_script
  when:
    - ansible_facts['distribution'] == 'Ubuntu'
    - shell_zsh_zprofile.stat.exists | default(false)

# ============================================================================
# BASH HISTORY AUTO-COMMIT (SYSTEM-WIDE)
# Commands are written to history immediately (not just on shell exit)
# ============================================================================

- name: Configure bash history auto-commit system-wide (Linux)
  ansible.builtin.copy:
    dest: /etc/profile.d/bash-history-autocommit.sh
    content: |
      # Bash history auto-commit: Commands saved immediately
      # System-wide configuration for all users

      if [ -n "$BASH_VERSION" ]; then
          shopt -s histappend                      # Append to history file, don't overwrite
          PROMPT_COMMAND="history -a${PROMPT_COMMAND:+; $PROMPT_COMMAND}"  # Write to history after each command
          export HISTSIZE=10000                    # Commands in memory
          export HISTFILESIZE=20000                # Commands in file
          export HISTCONTROL=ignoredups:erasedups  # No duplicate entries
          export HISTTIMEFORMAT="%F %T "           # Timestamp format
      fi
    mode: '0644'
  when: ansible_facts['distribution'] in ['Fedora', 'Ubuntu']

# macOS: scope zsh history settings to the USER's ~/.zshrc, not /etc/zshenv.
# The original `/etc/zshenv` write affected every zsh session for every user
# on the box and required root. ~/.zshrc is user-level, no sudo needed, and
# uses a marked block so re-runs are idempotent and the change is easy to
# spot or remove.
- name: Configure zsh history settings (macOS — user-level ~/.zshrc)
  ansible.builtin.blockinfile:
    path: "{{ user_home }}/.zshrc"
    create: true
    mode: '0644'
    marker: "# {mark} HYPERI: zsh history settings"
    block: |
      setopt APPEND_HISTORY        # Append to history file (don't overwrite)
      setopt INC_APPEND_HISTORY    # Write history immediately, not at shell exit
      setopt HIST_IGNORE_DUPS      # Skip immediate duplicates
      setopt HIST_FIND_NO_DUPS     # Don't show duplicates in search
      export HISTSIZE=10000        # Commands in memory
      export SAVEHIST=20000        # Commands in history file
      export HISTFILE="$HOME/.zsh_history"
  become: false
  when: ansible_facts['distribution'] == 'MacOSX'

# ============================================================================
# ls / grep COLOURISATION (SYSTEM-WIDE, opt-out per user)
# ============================================================================
# Uses only what each OS already ships - nothing installed. GNU ls (Linux, or
# macOS with brew coreutils) reads LS_COLORS; BSD ls (bare macOS) ignores it
# and reads LSCOLORS instead, so the block picks one at shell start.
#
# Colours are ANSI indices, not RGB, so they follow the terminal theme and need
# no light/dark handling of their own.
#
# Goes in the INTERACTIVE shell rc, not /etc/profile.d: aliases are not
# exported, and Ubuntu ships its profile.d hook commented out in
# /etc/bash.bashrc, so a terminal's non-login shell never reads it.
#
# Fedora already sets this system-wide via coreutils' colorls.sh, so there the
# block just sets the same thing again. On macOS /etc/zshrc is Apple's file and
# an OS update can replace it -- re-run the role after a major upgrade.
#
# Scope is soe_ls_colour_scope (see defaults/main.yml): 'system' for every
# account on the box, 'user' for just the invoking one.
- name: Resolve the ls/grep colour block and its target
  ansible.builtin.set_fact:
    soe_ls_colour_block: |
      # Colour ls/grep using whatever this OS already ships - no packages.
      # GNU (Linux, or macOS with brew coreutils) reads LS_COLORS and needs
      # --color. BSD (bare macOS) reads LSCOLORS and needs CLICOLOR.
      if command -v dircolors >/dev/null 2>&1; then
          eval "$(dircolors -b)"
          alias ls='ls --color=auto'
          alias grep='grep --color=auto'
      else
          export CLICOLOR=1
          export LSCOLORS=ExFxBxDxCxegedabagacad
      fi
    soe_ls_colour_path: >-
      {{
        (user_home ~ '/.zshrc') if soe_ls_colour_scope == 'user' and soe_mac
        else (user_home ~ '/.bashrc') if soe_ls_colour_scope == 'user'
        else '/etc/zshrc' if soe_mac
        else '/etc/bashrc' if ansible_facts['distribution'] == 'Fedora'
        else '/etc/bash.bashrc'
      }}
  vars:
    soe_mac: "{{ ansible_facts['distribution'] == 'MacOSX' }}"

- name: Enable ls/grep colour
  ansible.builtin.blockinfile:
    path: "{{ soe_ls_colour_path }}"
    create: true
    mode: '0644'
    marker: "# {mark} HYPERI: ls colors"
    block: "{{ soe_ls_colour_block }}"
  become: "{{ soe_ls_colour_scope == 'system' }}"
