---
# Developer-Python -- the Python tier.
#
# The Astral suite (uv + ruff + ty) lives in the `astral` role, which the base
# already pulls in. Depend on it so `--tags developer-python` on its own still
# gets uv/ruff/ty rather than assuming the base ran.

dependencies:
  - role: astral
