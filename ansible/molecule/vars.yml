---
# SSoT for the molecule test matrix.
#
# CONFIG-DRIVEN, NOT HARDCODED -- both n-1 slots roll within a year, so this is
# the one place to bump the MATRIX. We support n and n-1, and the declared
# minimum IS n-1.
#
# It does NOT feed the playbook's declared minimums. `min_fedora_version` and
# `min_ubuntu_version` live in roles/developer/defaults/main.yml and must be
# bumped WITH this file, by hand, until S1.T0c gives the OS gate a real SSoT.
# This is not hypothetical: this file said Fedora 43 was n-1 while the role
# still pinned min_fedora_version to 42, two months after 42 went EOL. An
# earlier version of this comment claimed the two "cannot drift apart" -- they
# had already drifted, and the claim is why nobody looked.
#
# WHY n AND n-1 ONLY (not n-2):
#   Ubuntu LTS lives 5 years, so n-2 (22.04) would be fine. Fedora only ever
#   supports TWO releases (~13 months), so Fedora's n-2 is ALWAYS EOL by
#   construction and its mirrors move to archives.fedoraproject.org — a test
#   there fails on dnf, not on our playbook. One rule for both distros beats
#   a per-distro exception.
#
# Verified 2026-07-17 (endoflife.date):
#   Ubuntu 26.04 Resolute Raccoon  2026-04-23, EOL 2031-04-30
#   Ubuntu 24.04 Noble Numbat      2024-04-25, EOL 2029-05-31
#   Fedora 44                      2026-04-28, EOL 2027-06-02
#   Fedora 43                      2025-10-28, EOL 2026-12-09
#   (Fedora 42 EOL'd 2026-05-27 — already dead, hence no n-2.)

molecule_matrix:
  - name: ubuntu-2604
    image: docker.io/library/ubuntu:26.04
    distro: Ubuntu
    release: "26.04"
    rank: n
  - name: ubuntu-2404
    image: docker.io/library/ubuntu:24.04
    distro: Ubuntu
    release: "24.04"
    rank: n-1
  - name: fedora-44
    image: docker.io/library/fedora:44
    distro: Fedora
    release: "44"
    rank: n
  - name: fedora-43
    image: docker.io/library/fedora:43
    distro: Fedora
    release: "43"
    rank: n-1

# The old tag the remediation scenario provisions FROM, to manufacture drift.
# Bump this as old releases fall out of the field. Anything older than this is
# out of scope for automated remediation testing.
remediation_from_tag: "v2.4.4"
